app = "tene-api"
primary_region = "fra"

[build]
dockerfile = "Dockerfile"

[vm]
memory = 1024
cpu_kind = "shared"
cpus = 1

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = true
auto_start_machines = true
